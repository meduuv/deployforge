# Configuration validation

Validate required configuration before starting a deployment.

Fail early when required values are missing or malformed. Treat credentials as secrets, and report configuration errors without printing their values.
